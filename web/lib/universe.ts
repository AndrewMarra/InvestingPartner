// Mirrors config.yaml `research.candidate_universe` — the curated default set the
// worker watches when a user hasn't customised their own list. Keep in sync.
export const DEFAULT_UNIVERSE = [
  "SPY", "QQQ", "IWM", "NVDA", "AVGO", "MSFT", "GOOGL", "AMZN", "META",
  "JPM", "XLE", "XLV", "LLY", "COST", "GLD",
];
